## 0으로 초기화
* 모든 객체를 0으로 초기화한다.
* 열거형에 0이 없더라도 0으로 초기화되기 때문에 제대로 동작하지 않을 수 있다.
* 열거형의 0에 유효한 값이 들어가는 것은 이상하기 때문에 None = 0으로 만들어주는게 좋다.
* 플래그로 사용하는 열거형은 항상 None을 0으로 설정해야한다. 비트 연산시에 0은 큰 문제를 일으키기 때문
* string도 get 속성에서 String.Empty를 리턴해주는게 좋다.